import { getSession } from "@/lib/auth"; import { redirect } from "next/navigation"; import LoginForm from "@/components/LoginForm";
export default async function Login(){const session=await getSession(); if(session)redirect('/portal'); return <main className="loginPage"><section className="brandPanel"><div className="brandWord">ALEMSI</div><div className="brandLine"/><p>Servicios de Higiene y Desinfección</p><div className="heroCopy"><span>Complejo Fronterizo · Araucanía</span><h1>Sistema de Alimentación<br/><em>Mamuil Malal</em></h1><p>Reservas, coordinación, finanzas y producción en un entorno corporativo protegido.</p></div></section><section className="loginPanel"><div className="loginCard"><span className="security">● ACCESO SEGURO</span><h2>Personal ALEMSI</h2><p>Ingresa con las mismas credenciales operativas de la versión Streamlit.</p><LoginForm/><div className="divider"><span>o</span></div><a className="secondary" href="/comensal">Ingresar como comensal</a><small>Migración paralela RC8 · Streamlit permanece operativo.</small></div></section></main>}
