import AppShell from '@/components/AppShell'; import { requireUser } from '@/lib/auth/session'; import { dashboardGerencia } from '@/lib/db/gerencia'; export const dynamic='force-dynamic'; export default async function Page(){ const u=await requireUser(['Gerencia','AdminTotal']); const d=await dashboardGerencia(); return <AppShell user={u}><div className="space-y-5"><section className="grid gap-3 md:grid-cols-2"><div className="rounded-2xl border bg-white p-5"><div className="text-sm text-[#6B7570]">Reservas futuras</div><div className="text-3xl font-black">{d.resumen.reservas||0}</div></div><div className="rounded-2xl border bg-white p-5"><div className="text-sm text-[#6B7570]">Valor asociado</div><div className="text-3xl font-black">${Number(d.resumen.monto||0).toLocaleString('es-CL')}</div></div></section><section className="rounded-2xl border bg-white p-5"><h2 className="text-xl font-black">Estado financiero</h2><div className="mt-3 grid gap-2 md:grid-cols-3">{d.pagos.map((r:any)=><div key={r.estado} className="rounded-xl bg-[#F6F3EA] p-3"><b>{r.estado}</b><div>{r.cantidad} registros · ${Number(r.monto||0).toLocaleString('es-CL')}</div></div>)}</div></section><section className="rounded-2xl border bg-white p-5"><h2 className="text-xl font-black">Última auditoría</h2><div className="mt-3 space-y-2 text-sm">{d.auditoria.map((r:any,i:number)=><div key={i} className="border-b pb-2"><b>{r.accion}</b> · {r.usuario} · {r.fecha}<div className="text-[#6B7570]">{r.tabla} · {r.detalle}</div></div>)}</div></section></div></AppShell>}
